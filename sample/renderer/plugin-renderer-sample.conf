{
    "plugins": {
	// Load openvswitch renderer plugin
        "renderer": ["libopflex_agent_renderer_sample.so"]
    },

    "renderers": {
        // Sample renderer
        // EXAMPLE:
        "sample": {
	//    Put configuration specific to renderer plugin here.
        }
    }
}
