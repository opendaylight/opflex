language: c++
compiler:
  - gcc
os:
  - linux
addons:
  apt:
    packages:
      - libuv1-dev
      - libboost-all-dev
      - libssl-dev
      - rapidjson-dev
      - autoconf
      - openjdk-8-jdk-headless
      - maven
      - doxygen
      - texlive-font-utils
      - python-six

env:
  - OPT="--enable-tsan"

script: ./.travis/tsan-build.sh $OPT
